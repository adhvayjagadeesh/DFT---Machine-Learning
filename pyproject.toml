[project]
name = "dftml"
description = "Benchmarking ML models for predicting band gap of 2D material without CBM & VBM"
version = "67"
dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
  "scikit-optimize",
  "matplotlib",
  "xgboost",
]
requires-python = ">=3.9"
readme = "README.md"

[tool.ruff]
indent-width = 2
